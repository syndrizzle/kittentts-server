# Core server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0

# Data validation and serialization
pydantic>=2.0.0

# Logging and utilities
loguru>=0.7.0

# Note: KittenTTS must be installed separately
# Follow the KittenTTS installation instructions from their repository
